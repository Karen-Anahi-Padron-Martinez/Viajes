import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.agenciaviajes',
  appName: 'AgenciaViajes',
  webDir: 'dist/media/browser',
  bundledWebRuntime: false
};

export default config;
